image: kern_corridor.pgm
mode: trinary
resolution: 0.05
# The world's origin is the map's centre, so the map's lower-left corner sits at
# (-10, -5). A goal at x = 6 m is therefore 6 m ahead of the robot's start.
origin: [-10.0, -5.0, 0.0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.25
